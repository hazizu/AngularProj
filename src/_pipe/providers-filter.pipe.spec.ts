import { ProvidersFilterPipe } from './providers-filter.pipe';

describe('ProvidersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProvidersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular-proj-perso',
  webDir: 'dist/AngularProjPerso',
  bundledWebRuntime: false
};

export default config;
